# frozen_string_literal: true

Gem::Specification.new do |spec|
    spec.name          = "fort_pass"
    spec.version       = "0.1.0"
    spec.authors       = ["Edmilson Barreto"]
    spec.email         = ["seu@email.com"]
  
    spec.summary       = "Gerador de senhas aleatórias seguras."
    spec.description   = "Uma gem simples para gerar senhas fortes e seguras."
    spec.homepage      = "https://github.com/seuusuario/fort_pass"
    spec.license       = "MIT"
  
    spec.files         = Dir["lib/**/*.rb"]
    spec.require_paths = ["lib"]
  end
  